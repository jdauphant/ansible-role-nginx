---

env:
 RUNLEVEL: 1

nginx_redhat_pkg:
  - nginx

nginx_ubuntu_pkg:
  - python-selinux
  - nginx

nginx_installation_types_using_service: ["packages"]

nginx_is_el: "{{ ansible_distribution in ['RedHat', 'CentOS'] }}"
