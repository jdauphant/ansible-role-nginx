#{{ ansible_managed }}
server {

{% for v in item.server.vars %}
   {{ v.replace(";",";\n      ").replace("{","{\n      ").replace("}","\n   }\n") }}{% if v.find('{') == -1%} ;{% endif %} 
{% endfor %}

}
