#{{ ansible_managed }}
server {

{% for v in nginx_sites[item] %}
   {{ v.replace(";",";\n      ").replace("{","{\n      ").replace("}","\n   }\n") }}{% if v.find('{') == -1%};{% endif %} 
{% endfor %}

}
